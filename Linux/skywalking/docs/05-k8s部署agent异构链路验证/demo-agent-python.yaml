apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-agent-python
  namespace: k8s-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-agent-python
  template:
    metadata:
      labels:
        app: demo-agent-python
    spec:
      containers:
        - name: app
          image: registry.cn-hangzhou.aliyuncs.com/zhengqingya/test:demo-agent-python
          imagePullPolicy: Always
          env:
            - name: HTTP_HOST
              value: "0.0.0.0"
            - name: HTTP_PORT
              value: "18081"
            - name: SW_AGENT_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app']
            - name: SW_AGENT_PROTOCOL
              value: "grpc"
            - name: SW_AGENT_COLLECTOR_BACKEND_SERVICES
              value: "skywalking-oap.skywalking.svc.cluster.local:11800"
          ports:
            - name: http
              containerPort: 18081
---
apiVersion: v1
kind: Service
metadata:
  name: demo-agent-python
  namespace: k8s-agent
spec:
  selector:
    app: demo-agent-python
  type: LoadBalancer
  ports:
    - name: http
      port: 31083
      targetPort: 18081
      protocol: TCP
